When running the program, it will use the set pattern within "pattern.txt". To change the pattern loaded into the maze, change this file. The pattern must always be square.

Once in the program with a loaded maze, press 'e' to exit, 'n' to view the pathfinding algorithm path, and anything else to create a new maze.
